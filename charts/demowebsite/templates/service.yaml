apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceName }}
  namespace: {{ .Release.Namespace }}
  labels:
    name: {{.Release.Name}}
spec:
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: {{.Release.Name}}