apiVersion: v1
kind: Service
metadata:
  name: {{ include "demowebsite.fullname" . }}
  labels:
    {{- include "demowebsite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demowebsite.selectorLabels" . | nindent 4 }}
